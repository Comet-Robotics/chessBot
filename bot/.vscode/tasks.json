{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Autoformat Code",
      "type": "shell",
      "command": "clang-format -i include/chessbot/* main/src/*",
      "problemMatcher": []
    },
    {
      "label": "Deploy OTA Update",
      "type": "shell",
      "command": "${config:idf.pythonBinPath} support.py ota",
      "options": {
        "env": {
          "PATH": "${env:PATH}:${config:idf.customExtraPaths}"
        }
      }
    }
  ]
}