/**
 * Defines a type of session.
 */
export enum ClientType {
    HOST = "HOST",
    CLIENT = "CLIENT",
    SPECTATOR = "SPECTATOR",
}

/**
 * Difficulties available for AI.
 */
export enum Difficulty {
    BABY = 0,
    BEGINNER = 1,
    INTERMEDIATE = 2,
    ADVANCED = 3,
}
